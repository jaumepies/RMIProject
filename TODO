TODO list:
    -OPTIONS:
        ->Search
        ->Exit(Return)
        ->Delete
        ->Revisar Download
    -USERS:
        X->Guardar ultim ID
        ->Eliminar subscripcio
        ->Afegir subscripcio
    -CALLBACKS:
        ->Modificar el Vector() per guardar el callbackObject amb el nom del usuari al que pertany.
        ->Al metode "registerForCallback" tenir en compte el usuari conectat actualment.
        ->Al metode "unregisterForCallback" eliminar el usuari que s'ha desconectat.
        ->Quan es fa un Upload cridar al "notifyMe" de cada client que te la subscripcio del objecte carregat.
        ->Sincronitzacio (semafors). UPDATE I DOWNLOAD.
