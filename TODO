TODO list:
    -OPTIONS:
        X->Search
        X->Exit(Return)
        X->Delete
        X->Revisar Download
    -USERS:
        X->Guardar ultim ID
        ->Eliminar subscripcio
        ->Afegir subscripcio
        ->Controlar al registre una contrasenya amb longitud > 4 caracters
    -CALLBACKS:
        X->Modificar el Vector() per guardar el callbackObject amb el nom del usuari al que pertany.
        X->Al metode "registerForCallback" tenir en compte el usuari conectat actualment.
        X->Al metode "unregisterForCallback" eliminar el usuari que s'ha desconectat.
        ->Quan es fa un Upload cridar al "notifyMe" de cada client que te la subscripcio del objecte carregat.
        ->Sincronitzacio (semafors). UPDATE I DOWNLOAD.
